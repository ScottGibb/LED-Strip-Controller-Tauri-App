export type RGB = { r: number; g: number; b: number };
export enum RGBColourComponent {
  R = "r",
  G = "g",
  B = "b",
}
export type HSV = { h: number; s: number; v: number };
export enum HSVColourComponent {
  H = "h",
  S = "s",
  V = "v",
}
